# Public site URL, used in metadata/canonical/sitemap/JSON-LD.
NEXT_PUBLIC_SITE_URL=https://www.turaway.com.bd

# Add real API base URLs / keys here once features/*/data/*.ts mocks are
# swapped for live fetches (flights, hotels, tours, visa, blog CMS, etc).
# NEXT_PUBLIC_API_BASE_URL=
